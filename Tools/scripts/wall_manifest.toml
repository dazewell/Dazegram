# Data for compose_walls.py -- see that script's header for how to run it.
#
# Each [[wall]] becomes one PNG under `output_dir`, built from an ordered list
# of [[wall.panel]] entries. A panel names a source screenshot (relative to
# `screenshots_dir`), a `crop` rectangle in that screenshot's native pixel
# coordinates as [left, top, right, bottom], and a one-line `caption`.
#
# Optional per-panel `redact`: a list of [left, top, right, bottom] rectangles,
# also in the *source* screenshot's native coordinates (not crop-relative),
# painted solid black before the crop is taken. Use this to black out anything
# in a screenshot that must not be published (e.g. a personal deep link).
#
# Optional per-panel `blur`: same rectangle format and coordinate space as
# `redact`, applied before the crop as well, but Gaussian-blurred instead of
# blacked out -- the shape stays roughly readable as "a message" or "a photo"
# while its actual content (real names, message text, a real profile photo)
# becomes illegible. Use this instead of `redact` where blacking the region
# out entirely would look wrong (e.g. a chat bubble full of real text).
#
# Panels are laid out left to right in a single row. Every panel keeps its
# crop's native aspect ratio; the tool derives each panel's on-canvas size
# from the layout rules in [settings] rather than from anything specified here.
#
# A [[figure]] is a different, simpler shape for FEATURES.md: a single plain
# cropped screenshot with no canvas, caption, or shadow, written to
# `figures_dir` as `output`. It reuses the same `source`/`crop`/`redact`/
# `blur` fields and coordinate space as a wall panel, then is downscaled
# (never upscaled) so its longest side is at most FIGURE_MAX_DIMENSION
# pixels in compose_walls.py.

[settings]
canvas_width = 1680
canvas_height = 1000
margin = 60                 # safe margin on all four canvas edges
gutter = 40                 # horizontal gap between adjacent cards
corner_radius = 24          # panel corner radius at final on-canvas size
shadow_blur = 30            # Gaussian blur radius for the drop shadow
shadow_offset_y = 12        # vertical offset of the drop shadow
shadow_opacity = 0.20       # drop shadow opacity, 0..1 (of solid black)
bg_top = "#121C31"          # canvas background gradient, top
bg_bottom = "#1A304A"       # canvas background gradient, bottom
caption_color = "#7AB2FF"
caption_font_size = 34
caption_gap = 18            # gap between card bottom and top of caption text
stagger_amplitude = 35      # +/- vertical offset alternated across panels
screenshots_dir = "docs/screenshots"   # gitignored, local-only raw captures
output_dir = "docs/images"             # committed composited wall output
figures_dir = "docs/images/features"   # committed plain-crop figure output

[[wall]]
output = "hero.png"

[[wall.panel]]
source = "time-zone-sheet-point-in-time.png"
crop = [0, 662, 1080, 2192]
caption = "Compare hours"

[[wall.panel]]
source = "time-zone-sheet-range-selected.png"
crop = [0, 566, 1080, 2192]
caption = "Pick a range"

[[wall.panel]]
source = "time-zone-schedule-peer-tab.png"
crop = [0, 624, 1080, 2192]
caption = "Schedule for them"

[[wall.panel]]
source = "time-zone-chat-header-pill-and-message-time.png"
crop = [0, 0, 1080, 1010]
caption = "See it in the chat"
# This crop is a real conversation; blur out the reply-quote author name
# (red text) so only the UI chrome (header pill, dual timestamps) stays
# legible. Everything else in the shot is throwaway test content.
blur = [
    [40, 495, 300, 560],   # reply-quote author name
]

[[wall]]
output = "chat-privacy.png"

[[wall.panel]]
source = "chat-privacy-hidden-vs-normal-chat-list.png"
crop = [0, 0, 1080, 1150]
caption = "Hidden from the list"

[[wall.panel]]
source = "chat-privacy-sheet-both-switches-on.png"
crop = [0, 580, 1080, 1545]
caption = "Your call, per chat"
# Re-shot 2026-09-08 after the sheet was redesigned with collapsible
# sections; the old crop coordinates no longer line up with the new
# layout. A short crop here previously starved the whole wall's shared
# card height (every panel in a wall shares one height, driven by the
# summed aspect ratio of all panels -- a short/wide panel here shrinks
# every card). This crop starts just below the sheet's rounded top edge
# (well clear of the real name/face in the chat behind it) and runs
# through both switches ("Hide last message" and "Require password"),
# the "Notifications" section, the "Disguise notifications" row, and
# the "How covers work" row, ending cleanly before its expander body.

[[wall.panel]]
source = "chat-privacy-disguise-notification.png"
crop = [0, 0, 510, 202]
caption = "Or disguise the alert"

[[wall]]
output = "composer.png"

[[wall.panel]]
source = "composer-toolbar-live-in-chat-text-selected.png"
crop = [0, 0, 1080, 1345]
caption = "Actions under the field"

[[wall.panel]]
source = "composer-layout-editor-drag-list.png"
crop = [0, 545, 1080, 1678]
caption = "Arrange it yourself"

[[wall.panel]]
source = "composer-layout-editor-sliders.png"
crop = [0, 0, 1080, 1445]
caption = "Size and spacing"

[[wall.panel]]
source = "fullscreen-input-expanded.png"
crop = [0, 140, 1080, 1330]
caption = "Expand to full screen"

[[wall]]
output = "video-recording.png"

[[wall.panel]]
source = "pre-video-message-options-infinite-recording-and-external-mic.jpg"
crop = [0, 900, 1080, 2010]
caption = "Set up before you record"

[[wall.panel]]
source = "video-message-buttons-layout.jpg"
crop = [0, 1590, 1080, 2354]
caption = "Controls while recording"
# Starts cleanly below the round preview circle (its bottom edge sits at
# native y=1583, verified against the source) rather than slicing through
# it -- this panel is about the control row, not the preview.

[[wall.panel]]
source = "video-message-preview-scrub-midpoint.png"
crop = [0, 1600, 1080, 2192]
caption = "Scrub the preview"

[[wall]]
output = "video-watching.png"

[[wall.panel]]
source = "video-message-player-panel-mute-and-loop.png"
crop = [0, 0, 1080, 500]
caption = "Mute and playback modes"
# Cropped above the real face rather than blurred through it: this includes
# the chat header and the player panel and stops right before the hair
# begins (verified against the source -- no column has hair/skin above
# y=506), so no blur or redaction is needed. The player-panel controls are
# the subject of this panel; the video content underneath isn't.

[[wall.panel]]
source = "video-message-closed-captions-playback.png"
crop = [0, 1300, 1080, 1930]
caption = "Captions on playback"
# Cropped below the real face rather than blurred through it: the face ends
# around y=1280 (verified against the source), and everything the panel
# needs -- the burned-in subtitle and the transcription bubble below it --
# sits well below that, so no blur or redaction is needed.

[[wall]]
output = "privacy-profiles.png"

[[wall.panel]]
source = "privacy-profiles-config.jpg"
crop = [0, 324, 1080, 1651]
caption = "Profiles & timeouts"
redact = [[50, 1565, 690, 1622]]

[[wall.panel]]
source = "privacy-profiles-easy-switch.jpg"
crop = [100, 935, 655, 2130]
caption = "Switch in one tap"

# --- FEATURES.md figures ------------------------------------------------
#
# Plain cropped screenshots (no canvas/caption/shadow), replacing the two
# GitHub user-attachments CDN images previously on the #timezones entry so
# every FEATURES.md figure is versioned in the repo instead. Raw sources
# are the gitignored captures staged under docs/screenshots/new/ for this
# change; see that directory's shot list for what each one depicts.

[[figure]]
output = "timezones-1.png"
source = "new/screenshot_2026-09-06_16-58-55.png"
crop = [0, 1130, 1080, 2010]
# Profile edit entry point: the Time Zone row plus the "Select Time Zone"
# sheet (title, search, list) that opens from it. No sensitive content.

[[figure]]
output = "timezones-2.png"
source = "new/screenshot_2026-09-06_17-01-16.png"
crop = [0, 555, 1080, 758]
# The chat list row showing the per-chat clock pill next to the contact
# name. "DazeTest" is a fake demo persona, not a real name.

[[figure]]
output = "reschedule.png"
source = "new/screenshot_2026-09-06_17-04-25.png"
crop = [0, 215, 1080, 2180]
# The Reschedule sheet: date/time grid, interval slider, delay, and the
# Reschedule button. All generic UI text, no sensitive content.

[[figure]]
output = "scheduled-selection-toolbar.png"
source = "new/screenshot_2026-09-06_17-04-37.png"
crop = [0, 0, 1080, 130]
# Just the top selection toolbar in the scheduled view: Send Now,
# Reschedule, Delete, and the overflow menu (Copy/Forward tucked inside).

[[figure]]
output = "trigger-editor.png"
source = "new/screenshot_2026-09-08_17-32-33.png"
crop = [0, 1170, 1080, 2192]
# The redesigned trigger editor's collapsible sections ("By message type",
# "Or by text"), collapsed, plus the delay slider and Cancel/Done. This
# is the only trigger-editor capture used -- see FEATURES.md PR notes on
# why the original (pre-redesign) capture was dropped.

[[figure]]
output = "message-triggers-list.png"
source = "new/screenshot_2026-09-06_17-13-47.png"
crop = [0, 0, 1080, 1080]
# The Message Triggers list: header, two fake trigger cards (BotFather and
# DazeTest test personas, not real contacts), and the footer disclaimer.

[[figure]]
output = "bulk-actions.png"
source = "new/screenshot_2026-09-08_15-58-55.png"
crop = [0, 690, 1080, 1900]
# Five consecutive replies in a staged demo conversation, numbered 1-5, to
# show bulk pin/number-several-at-once. Blurred: three reply-quote author
# names (a real name reused across this staged conversation) that would
# otherwise be legible in the crop; everything else is throwaway test text.
blur = [
    [700, 860, 950, 920],
    [190, 1285, 460, 1345],
    [190, 1710, 460, 1770],
]

[[figure]]
output = "monet-pattern.png"
source = "new/screenshot_2026-09-08_16-14-27.png"
crop = [0, 0, 1080, 1930]
# The Monet-tinted wallpaper pattern visible in the chat's negative space.
# Blurred: one reply-quote author name near the top of the same staged
# demo conversation as bulk-actions.png above.
blur = [[60, 165, 290, 215]]

[[figure]]
output = "extera-light.png"
source = "new/screenshot_2026-09-08_16-14-48.png"
crop = [0, 0, 1080, 1930]
# Extera theme, light variant, same staged demo conversation. Blurred: a
# partial reply-quote author name near the top-left, cut by the back
# button but still legible enough to redact.
blur = [[100, 40, 260, 95]]

[[figure]]
output = "extera-dark.png"
source = "new/screenshot_2026-09-08_16-15-40.png"
crop = [0, 0, 1080, 1930]
# Extera theme, dark variant, same scroll position as extera-light.png.
# Blurred: the reply-quote author name, fully legible here (unlike the
# partial/cropped occurrence in extera-light.png).
blur = [[40, 158, 270, 210]]

[[figure]]
output = "transcribe-retry.png"
source = "new/screenshot_2026-09-08_17-26-27.png"
crop = [60, 795, 920, 1372]
# Cropped tightly to just the "Retry with..." provider dialog, excluding
# the chat background entirely -- which is how a real face (a round-video
# thumbnail) and a real account name in the header are avoided without
# needing blur or redaction.

[[figure]]
output = "repost-copy.png"
source = "new/screenshot_2026-09-08_17-29-00.png"
crop = [0, 1730, 1080, 2000]
# The "Reposted as a copy" notice bar. Blurred: a sliver of a reply-quote
# author name peeking above the notice bar's rounded top edge.
blur = [[140, 1695, 360, 1750]]

[[figure]]
output = "cite.png"
source = "new/screenshot_2026-09-08_17-30-38.png"
crop = [0, 1598, 1080, 2060]
# Cropped to just the composer: the typed text plus two stacked cited
# quote blocks. This excludes the message history above (which contains a
# real name in this conversation) entirely, so no blur is needed.

[[figure]]
output = "reply-threads.png"
source = "new/screenshot_2026-09-08_17-31-17.png"
crop = [0, 240, 1080, 570]
# Two consecutive bubbles showing the small reply-glyph-plus-count marker
# (distinct from the large bulk-numbering digits used in bulk-actions.png)
# going from 1 to 2. Blurred: the one reply-quote author name in this crop.
blur = [[40, 370, 340, 430]]
