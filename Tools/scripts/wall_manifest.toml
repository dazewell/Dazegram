# Data for compose_walls.py -- see that script's header for how to run it.
#
# Each [[wall]] becomes one PNG under `output_dir`, built from an ordered list
# of [[wall.panel]] entries. A panel names a source screenshot (relative to
# `screenshots_dir`), a `crop` rectangle in that screenshot's native pixel
# coordinates as [left, top, right, bottom], and a one-line `caption`.
#
# Optional per-panel `redact`: a list of [left, top, right, bottom] rectangles,
# also in the *source* screenshot's native coordinates (not crop-relative),
# painted solid black before the crop is taken. Use this to black out anything
# in a screenshot that must not be published (e.g. a personal deep link).
#
# Optional per-panel `blur`: same rectangle format and coordinate space as
# `redact`, applied before the crop as well, but Gaussian-blurred instead of
# blacked out -- the shape stays roughly readable as "a message" or "a photo"
# while its actual content (real names, message text, a real profile photo)
# becomes illegible. Use this instead of `redact` where blacking the region
# out entirely would look wrong (e.g. a chat bubble full of real text).
#
# Panels are laid out left to right in a single row. Every panel keeps its
# crop's native aspect ratio; the tool derives each panel's on-canvas size
# from the layout rules in [settings] rather than from anything specified here.

[settings]
canvas_width = 1680
canvas_height = 1000
margin = 60                 # safe margin on all four canvas edges
gutter = 40                 # horizontal gap between adjacent cards
corner_radius = 24          # panel corner radius at final on-canvas size
shadow_blur = 30            # Gaussian blur radius for the drop shadow
shadow_offset_y = 12        # vertical offset of the drop shadow
shadow_opacity = 0.20       # drop shadow opacity, 0..1 (of solid black)
bg_top = "#121C31"          # canvas background gradient, top
bg_bottom = "#1A304A"       # canvas background gradient, bottom
caption_color = "#7AB2FF"
caption_font_size = 34
caption_gap = 18            # gap between card bottom and top of caption text
stagger_amplitude = 35      # +/- vertical offset alternated across panels
screenshots_dir = "docs/screenshots"   # gitignored, local-only raw captures
output_dir = "docs/images"             # committed composited output

[[wall]]
output = "hero.png"

[[wall.panel]]
source = "time-zone-sheet-point-in-time.png"
crop = [0, 662, 1080, 2192]
caption = "Compare hours"

[[wall.panel]]
source = "time-zone-sheet-range-selected.png"
crop = [0, 566, 1080, 2192]
caption = "Pick a range"

[[wall.panel]]
source = "time-zone-schedule-peer-tab.png"
crop = [0, 624, 1080, 2192]
caption = "Schedule for them"

[[wall.panel]]
source = "time-zone-chat-header-pill-and-message-time.png"
crop = [0, 0, 1080, 1010]
caption = "See it in the chat"
# This crop is a real conversation; blur out the reply-quote author name
# (red text) so only the UI chrome (header pill, dual timestamps) stays
# legible. Everything else in the shot is throwaway test content.
blur = [
    [40, 495, 300, 560],   # reply-quote author name
]

[[wall]]
output = "chat-privacy.png"

[[wall.panel]]
source = "chat-privacy-hidden-vs-normal-chat-list.png"
crop = [0, 0, 1080, 1150]
caption = "Hidden from the list"

[[wall.panel]]
source = "chat-privacy-sheet-both-switches-on.png"
crop = [0, 790, 1080, 1858]
caption = "Your call, per chat"

[[wall.panel]]
source = "chat-privacy-disguise-notification.png"
crop = [0, 0, 510, 202]
caption = "Or disguise the alert"

[[wall]]
output = "composer.png"

[[wall.panel]]
source = "composer-toolbar-live-in-chat-text-selected.png"
crop = [0, 0, 1080, 1345]
caption = "Actions under the field"

[[wall.panel]]
source = "composer-layout-editor-drag-list.png"
crop = [0, 545, 1080, 1678]
caption = "Arrange it yourself"

[[wall.panel]]
source = "composer-layout-editor-sliders.png"
crop = [0, 0, 1080, 1445]
caption = "Size and spacing"

[[wall.panel]]
source = "fullscreen-input-expanded.png"
crop = [0, 140, 1080, 1330]
caption = "Expand to full screen"

[[wall]]
output = "video-recording.png"

[[wall.panel]]
source = "pre-video-message-options-infinite-recording-and-external-mic.jpg"
crop = [0, 900, 1080, 2010]
caption = "Set up before you record"

[[wall.panel]]
source = "video-message-buttons-layout.jpg"
crop = [0, 1590, 1080, 2354]
caption = "Controls while recording"
# Starts cleanly below the round preview circle (its bottom edge sits at
# native y=1583, verified against the source) rather than slicing through
# it -- this panel is about the control row, not the preview.

[[wall.panel]]
source = "video-message-preview-scrub-midpoint.png"
crop = [0, 1600, 1080, 2192]
caption = "Scrub the preview"

[[wall]]
output = "video-watching.png"

[[wall.panel]]
source = "video-message-player-panel-mute-and-loop.png"
crop = [0, 0, 1080, 500]
caption = "Mute, loop, or speed it up"
# Cropped above the real face rather than blurred through it: this includes
# the chat header and the player panel and stops right before the hair
# begins (verified against the source -- no column has hair/skin above
# y=506), so no blur or redaction is needed. The player-panel controls are
# the subject of this panel; the video content underneath isn't.

[[wall.panel]]
source = "video-message-closed-captions-playback.png"
crop = [0, 1300, 1080, 1930]
caption = "Captions on playback"
# Cropped below the real face rather than blurred through it: the face ends
# around y=1280 (verified against the source), and everything the panel
# needs -- the burned-in subtitle and the transcription bubble below it --
# sits well below that, so no blur or redaction is needed.

[[wall]]
output = "privacy-profiles.png"

[[wall.panel]]
source = "privacy-profiles-config.jpg"
crop = [0, 324, 1080, 1651]
caption = "Profiles & timeouts"
redact = [[50, 1565, 690, 1622]]

[[wall.panel]]
source = "privacy-profiles-easy-switch.jpg"
crop = [100, 935, 655, 2130]
caption = "Switch in one tap"
